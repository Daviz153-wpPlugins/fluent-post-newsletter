# Fluent Post Newsletter — 프로젝트 기초 문서

> 워드프레스 포스트를 FluentCRM 이메일 캠페인으로 복제·발송하는 애드온 플러그인

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 플러그인명 | Fluent Post Newsletter |
| 슬러그 | `fluent-post-newsletter` |
| 유형 | FluentCRM 애드온 워드프레스 플러그인 |
| 대상 사용자 | 국내 뉴스레터 마케팅 실습 수강생 (비개발자 포함) |
| 배포 방식 | GitHub 릴리즈 + 자동 업데이트 |
| 의존성 | FluentCRM (필수), FluentSMTP (권장) |

---

## 2. 핵심 사용자 스토리

### 주 시나리오
```
작성자가 워드프레스 포스트를 작성한다
→ 포스트 편집 화면에서 수신자 태그를 선택한다
→ 전체 본문 / 요약 발췌 중 콘텐츠 방식을 선택한다
→ 즉시 발송 또는 예약 발송 날짜를 지정한다
→ "뉴스레터 생성" 버튼을 클릭한다
→ FluentCRM 캠페인이 자동 생성되고 설정한 시간에 발송된다
→ 구독자는 Ghost 스타일 이메일을 수신한다
→ "브라우저에서 보기" 클릭 시 포스트 원문으로 이동한다
```

### 재발송/수정 시나리오
```
포스트 수정 후 "뉴스레터 동기화" 버튼 클릭
→ 발송 전 캠페인이면 → 내용 업데이트
→ 이미 발송된 캠페인이면 → 업데이트 차단, 안내 메시지 표시
```

---

## 3. 기능 명세

### 3-1. 포스트 편집 화면 메타박스

| 필드 | 타입 | 설명 |
|------|------|------|
| 수신자 태그 | Multi-select | FluentCRM 태그 목록 동적 로드 |
| 콘텐츠 방식 | Radio | 전체 본문 / 요약 발췌 선택 |
| 발송 방식 | Radio | 즉시 발송 / 예약 발송 |
| 예약 일시 | datetime-local | 예약 발송 선택 시 노출 |
| 연결된 캠페인 | 읽기 전용 | 생성 후 FluentCRM 캠페인 ID + 상태 표시 |
| 뉴스레터 생성 버튼 | Button | 캠페인 생성/동기화 실행 |

### 3-2. 이메일 템플릿 (Ghost 스타일)

```
┌─────────────────────────────────────┐
│        사이트명 | 사이트 슬로건        │  ← wp_bloginfo 사용
├─────────────────────────────────────┤
│                                     │
│   포스트 제목 (대형 볼드)             │
│                                     │
│   작성자명 · YYYY년 M월 D일           │
│   브라우저에서 보기 (링크)            │
│                                     │
│   [대표 이미지 - 있을 경우]           │
│                                     │
│   본문 내용 (전체 또는 발췌)          │
│                                     │
│   [발췌 모드일 때만] → 원문 읽기 버튼 │
├─────────────────────────────────────┤
│   수신 거부 | © 사이트명              │  ← FluentCRM 기본 푸터
└─────────────────────────────────────┘
```

- 배경: 흰색 / 최대 너비: 600px / 중앙 정렬
- 폰트: system-ui, sans-serif (한글 렌더링 우선)
- 발신자 정보: FluentCRM 전역 설정값 그대로 사용
- 수신 거부 링크: FluentCRM 스마트코드 `{{crm.unsubscribe_url}}` 사용

### 3-3. 캠페인 생성 로직

```
[생성 버튼 클릭]
  │
  ├─ post_meta에 campaign_id 없음 → FluentCRM 캠페인 신규 생성
  │
  └─ post_meta에 campaign_id 있음
       ├─ 캠페인 상태 = draft/scheduled → 내용 업데이트
       └─ 캠페인 상태 = sent/processing → 차단 + 안내
```

### 3-4. 저장 데이터 (Post Meta)

| 메타키 | 값 |
|--------|----|
| `_fpn_campaign_id` | FluentCRM 캠페인 ID |
| `_fpn_campaign_status` | 마지막 확인 상태 |
| `_fpn_content_type` | `full` / `excerpt` |
| `_fpn_send_type` | `now` / `scheduled` |
| `_fpn_scheduled_at` | 예약 일시 (ISO 8601) |
| `_fpn_tags` | 선택된 태그 ID 배열 (JSON) |

---

## 4. Definition of Done (완료 기준)

### 기능 DoD
- [ ] 포스트 편집 화면에 메타박스가 정상 표시된다
- [ ] FluentCRM 태그 목록이 동적으로 로드된다
- [ ] 뉴스레터 생성 버튼 클릭 시 FluentCRM 캠페인이 생성된다
- [ ] 생성된 캠페인이 FluentCRM 캠페인 목록에 나타난다
- [ ] 즉시 발송이 정상 작동한다
- [ ] 예약 발송이 지정 시간에 정상 작동한다
- [ ] 발송 전 캠페인은 동기화(수정)가 가능하다
- [ ] 발송 후 캠페인은 수정이 차단되고 안내 메시지가 표시된다
- [ ] "브라우저에서 보기" 링크가 포스트 URL로 정확히 연결된다

### 이메일 렌더링 DoD (핵심 우려사항)
- [ ] Gmail (웹/앱)에서 레이아웃이 깨지지 않는다
- [ ] 네이버 메일에서 레이아웃이 깨지지 않는다
- [ ] 카카오 메일에서 레이아웃이 깨지지 않는다
- [ ] 모바일(iOS/Android) 메일 앱에서 반응형으로 표시된다
- [ ] 워드프레스 블록 에디터의 HTML 요소가 이메일에서 안전하게 렌더링된다
- [ ] 이미지가 있을 때/없을 때 모두 레이아웃이 정상이다
- [ ] 한글 폰트가 정상 표시된다

### 배포 DoD
- [ ] GitHub 릴리즈에서 `.zip` 다운로드 후 워드프레스에 설치 가능하다
- [ ] GitHub Updater 또는 자체 업데이트 로직으로 자동 업데이트된다
- [ ] FluentCRM 미설치 시 활성화가 차단되고 안내 메시지가 표시된다

---

## 5. 개발 원칙 (변경 불가)

### 코드 품질 원칙
1. **간결함 우선** — 기능 동작에 불필요한 코드는 작성하지 않는다. 추상화·헬퍼는 3회 이상 반복될 때만 도입한다.
2. **디버깅 용이성** — 에러 발생 위치를 즉시 파악할 수 있도록 책임을 명확히 분리한다. 클래스 하나에 역할 하나.
3. **보안 최우선** — 모든 사용자 입력은 반드시 검증/이스케이프. Nonce 없는 AJAX 요청은 코드에 존재할 수 없다.
4. **직관적 UI/UX** — 비개발자 수강생이 설명 없이 사용할 수 있어야 한다. 버튼 레이블, 상태 메시지, 오류 안내는 한국어로 명확하게.

### GitHub 보안 규칙 (절대 위반 불가)
- `.gitignore`에 아래 항목 반드시 포함:
  ```
  .env
  *.log
  /vendor/
  wp-config.php
  *-local.php
  .DS_Store
  ```
- 서버 IP, 도메인, DB 정보, API 키, 비밀번호는 코드에 하드코딩 금지
- 설정값은 워드프레스 옵션 API(`get_option`) 또는 상수로만 참조
- 커밋 전 `git diff --staged`로 개인정보 포함 여부 반드시 확인

---

## 6. 절대 규칙 (변경 불가)

1. **FluentCRM 리소스 우선 사용** — 자체 구현보다 FluentCRM의 API, 모델, 스마트코드, 훅을 최대한 활용한다. 중복 구현 금지.
2. **이메일 HTML은 인라인 CSS만 사용** — 이메일 클라이언트 호환성을 위해 `<style>` 태그 의존 금지. 모든 스타일은 인라인.
3. **발송된 캠페인은 절대 수정하지 않는다** — 상태가 `sent` 또는 `processing`인 캠페인에 대한 업데이트 요청은 코드 레벨에서 차단.
4. **포스트 메타에 캠페인 ID를 반드시 저장** — 중복 생성 방지의 유일한 키. 생성 즉시 저장, 예외 없음.
5. **발신자 정보는 FluentCRM 전역 설정 사용** — 별도 발신자 설정 UI 추가 금지.

---

## 7. 반드시 지켜야 하는 점

### 코드 품질
- PHP 8.2 호환 문법 사용
- 모든 사용자 입력은 `sanitize_*`, `wp_kses` 등으로 반드시 검증
- 워드프레스 Nonce를 이용한 AJAX 요청 검증 필수
- 직접 접근 차단: 모든 PHP 파일 상단에 `defined('ABSPATH') || exit;`

### FluentCRM 호환성
- FluentCRM 클래스 사용 시 `class_exists()` 체크 후 사용
- FluentCRM 내부 API 변경에 대비해 버전 체크 로직 포함
- FluentCRM 주요 버전 업데이트 시 이 플러그인도 호환성 테스트 및 업데이트

### 이메일 렌더링
- 블록 에디터 출력 HTML을 이메일용으로 정제하는 sanitizer 함수 필수 구현
  - 제거 대상: `<script>`, `<iframe>`, 워드프레스 블록 주석, `wp-block-*` 클래스
  - 유지 대상: `<p>`, `<h2>`~`<h4>`, `<ul>`, `<ol>`, `<li>`, `<a>`, `<img>`, `<blockquote>`, `<strong>`, `<em>`
- 모든 이미지는 절대 URL로 변환 (상대 경로 금지)
- 이미지에 `max-width: 100%` 인라인 스타일 강제 적용

---

## 8. 핵심 우려사항 및 대응 전략

### 우려 1: 포스트 본문 HTML이 이메일에서 깨진다 (최우선)

**원인 분석:**
- 블록 에디터는 `wp-block-*` 래퍼 div, SVG, 복잡한 class 구조 생성
- 이메일 클라이언트는 `<div>`, `class`, 외부 CSS를 무시하거나 제거
- 네이버/카카오 메일은 특히 CSS 지원이 제한적

**대응 전략:**
- `email_content_sanitizer()` 함수로 블록 HTML → 이메일 안전 HTML 변환
- `apply_filters('the_content', ...)` 후 커스텀 sanitizer 적용
- 허용 태그 화이트리스트 방식으로 처리
- 실제 이메일 클라이언트 테스트를 DoD의 통과 조건으로 설정

### 우려 2: FluentCRM 업데이트 시 호환성 깨짐

**대응 전략:**
- FluentCRM 내부 클래스 직접 인스턴스화 최소화
- 가능한 한 REST API 또는 공개 훅(`do_action`, `apply_filters`) 경유
- FluentCRM 버전을 플러그인 헤더에 `Tested up to` 형식으로 명시
- GitHub Actions로 FluentCRM 신규 릴리즈 감지 → 자동 호환성 테스트 (향후)

---

## 9. 기술 스택 & 환경

| 항목 | 사양 |
|------|------|
| WordPress | 7.0 |
| PHP | 8.2.27 (fpm-fcgi) |
| 웹서버 | Apache 2.4.62 |
| DB | MariaDB (Ubuntu 22.04) |
| 테마 | GeneratePress |
| 에디터 | Gutenberg (블록 에디터) |
| 이메일 발송 | FluentSMTP |
| CRM | FluentCRM |
| 서버 | Vultr |

---

## 10. 플러그인 파일 구조 (계획)

```
fluent-post-newsletter/
├── fluent-post-newsletter.php     # 메인 플러그인 파일
├── includes/
│   ├── class-plugin.php           # 플러그인 부트스트랩
│   ├── class-meta-box.php         # 포스트 메타박스
│   ├── class-campaign-manager.php # FluentCRM 캠페인 생성/업데이트
│   ├── class-email-template.php   # Ghost 스타일 이메일 템플릿
│   └── class-content-sanitizer.php# 블록 HTML → 이메일 안전 HTML
├── assets/
│   ├── js/
│   │   └── meta-box.js            # 메타박스 AJAX + UI
│   └── css/
│       └── meta-box.css           # 메타박스 스타일
└── languages/
    └── fluent-post-newsletter-ko_KR.po
```

---

## 11. 버전 계획

| 버전 | 내용 |
|------|------|
| 0.1.0 | 기본 캠페인 생성, 즉시/예약 발송, 태그 선택 |
| 0.2.0 | 이메일 템플릿 완성, HTML sanitizer |
| 0.3.0 | GitHub 자동 업데이트 |
| 1.0.0 | 배포용 — 전체 DoD 통과 후 |

---

*최종 업데이트: 2026-06-08*
